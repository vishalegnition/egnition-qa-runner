[phases.setup]
aptPkgs = ["xvfb", "x11-utils", "fonts-liberation"]

[phases.install]
cmds = ["npm ci", "npx patchright install chrome"]

[start]
cmd = "bash -c 'if command -v Xvfb; then Xvfb :99 -screen 0 1440x900x24 & export DISPLAY=:99; sleep 2; fi; exec node webhook/index.js'"
